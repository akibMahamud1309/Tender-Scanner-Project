# 10 --- MODULE CONFLICTS

Track conflicts between modules, Python versions, packages, frameworks,
browsers, OCR engines, databases, and APIs. Never blindly change global
dependencies. Record isolation and final known-good versions.
